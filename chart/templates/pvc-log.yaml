apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Chart.Name }}-log
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.config.storage.log }}
  storageClassName: standard
