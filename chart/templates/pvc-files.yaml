apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Chart.Name }}-files
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.config.storage.files }}
  storageClassName: standard
